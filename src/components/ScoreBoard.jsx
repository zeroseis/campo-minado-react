import React from 'react';

const ScoreBoard = (props) => (
  <div className="score-board">
    <div className="score -clear">Espaços liberados: X</div>
    <div className="score -exploded">Espaços detonados: X</div>
  </div>
);

export default ScoreBoard;